<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head', {
    title: `Редагувати ${course.title}`,
    }); %>

<body>
    <%- include('./partials/navbar',{
    activePage: 'add'
    }); %>
    <div class="container">
        <h1>
            Редагувати <%= course.title %>
        </h1>

        <form action="/courses/edit" method="POST">
            <div class="input-field ">
                <input placeholder="Title" id="title" name="title" type="text" class="validate" required
                    value="<%= course.title %>">
                <label for="title">Назва курсу</label>
                <span class="helper-text" data-error="Введіть назву"></span>
            </div>

            <div class="input-field ">
                <input placeholder="Price" id="price" name="price" type="text" class="validate" required min="1"
                    value="<%= course.price %>">
                <label for="price">Ціна</label>
                <span class="helper-text" data-error="Введіть ціну"></span>
            </div>

            <div class="input-field ">
                <input placeholder="Image" id="img" name="img" type="text" class="validate" required
                    value="<%= course.img %>">
                <label for="img">URL картинки</label>
                <span class="helper-text" data-error="Введіть url картинки"></span>
            </div>

            <input type="hidden" name="id" value="<%= course.id %>">

            <button type="submit" class="btn btn-primaty">Редагувати курс</button>
        </form>


    </div>
</body>

<%- include('./partials/footer'); %>


</html>