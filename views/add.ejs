<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head', {
    title: 'Додати курс',
    }); %>

<body>
    <%- include('./partials/navbar',{
    activePage: 'add'
    }); %>
    <div class="container">
        <h1>
            Добавить новый курс
        </h1>
        <% if (errors.length) { %>
            <p class="alert"><%= errors %> </p>
            <% } %>
        <form action="/add" method="POST">
            <div class="input-field ">
                <input placeholder="Title" id="title" name="title" type="text" class="validate" required value="<%= data.title %>" >
                <label for="title">Назва курсу</label>
                <span class="helper-text" data-error="Введіть назву"></span>
            </div>

            <div class="input-field ">
                <input placeholder="Price" id="price" name="price" type="text" class="validate" required min="1" value="<%= data.price %>" >
                <label for="price">Ціна</label>
                <span class="helper-text" data-error="Введіть ціну"></span>
            </div>

            <div class="input-field ">
                <input placeholder="Image" id="img" name="img" type="text" class="validate" required value="<%= data.img %>" >
                <label for="img">URL картинки</label>
                <span class="helper-text" data-error="Введіть url картинки"></span>
            </div>
            <input type="hidden" name="_csrf" value="<%= csrf %>">

            <button type="submit" class="btn btn-primaty">Додати курс</button>
        </form>


    </div>
</body>

<%- include('./partials/footer'); %>


</html>